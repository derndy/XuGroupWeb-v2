# Gallery image sizes and framework viewing — 6 September 2026

Based on main at `302eac67b1695434f67243aff4b62fd89f26f30a` (merged PR #20).

## Changes

All 23 Gallery cards now offer proportional WebP variants generated by Hugo from the existing original photographs, at 480, 800 and 1200 pixels where the original permits. No upscaling: the 1143-pixel original only supplies 480 and 800. There are 68 variants. Existing JPEG thumbnails remain the fallback; the viewer and original-photo links are unchanged. No originals, gallery metadata, consent status or captions were changed.

The sizes attribute follows the existing one/two/three-column breakpoints and the two-photo year layout. The picture fills the existing reserved aspect-ratio frame; captions remain below that frame. Variants use q85 and preserve the original aspect ratio; existing card cropping is unchanged. Actual browser selection depends on viewport and pixel density. The 23 smallest variants total 999,302 bytes; this is an asset-size measurement, not a page-speed test.

Each of the three research frameworks now offers direct links to its existing wide and vertical SVGs, with visible keyboard focus. Diagram geometry, labels and explanations are unchanged.

## Validation and limits

Production and preview Hugo builds pass (895 pages each). Both generated outputs contain 536 index pages; all 250 populated local image references resolve. All 68 Gallery WebP width descriptors resolve to correctly sized files; all six framework links resolve. Gallery visible text and all viewer data attributes match the previous output. Framework explanatory text matches after removing the six new links.

The seven approved-image / 28-WebP audit passes on both builds. The 91-publication baseline audit passes. The strict framework baseline assertion initially flagged the intentional addition of link text; the comparison above verifies the preserved explanation separately. No test expectation was weakened.

Browser viewport testing has not been performed. PyraE2E and the selected JACS paper still need source/reuse review before adding new paper figures. Legacy image descriptions remain a separate bounded review queue.

Delivery: public review branch `content/gallery-responsive-images`, draft PR only; no merge or production-setting changes.
